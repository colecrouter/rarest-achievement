{
	"name": "@project/worker",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"build": "npm run build -w @project/lib",
		"start": "wrangler dev",
		"test": "vitest run",
		"cf-typegen": "wrangler types",
		"check": "tsc --noEmit --skipLibCheck --pretty",
		"postinstall": "npm run cf-typegen",
		"db:migrate": "wrangler d1 migrations apply steam-data"
	},
	"devDependencies": {
		"@project/lib": "^1.0.0",
		"drizzle-orm": "0.45.2",
		"typescript": "^6.0.3",
		"vitest": "^4.1.4",
		"wrangler": "^4.95.0"
	},
	"peerDependencies": {
		"drizzle-orm": "0.42.0"
	}
}
